#include "main.h"
/**
 * _abs - computes  absolute value of an integer
 *
 * @n: The number to be computed
 * Return: Absolute value of a n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
