#include "main.h"
#include <stdio.h>
#include <unistd.h>
/**
 * _puts - prints out items in a string
 *
 * @s: string whose elements will be printed
 *
 * Return: printed string
 */
void _puts(char *s)
{
	;
}
/**
 * main - entry point
 *
 * Return: 1 or 0
 */
int main(void)
{
	return (0);
}
